import {LoadingSpinner} from './index';
import {defineComponentFixtures} from '../../testing/defineComponentFixtures';

export default defineComponentFixtures(LoadingSpinner, {
  fixtures: {
    md: {size: 'md'},
  },
});
